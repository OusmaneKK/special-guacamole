# GitHub Actions Workflow
# Generated with Pipeflow - https://pipeflow.app
# Language(s): 

name: CI/CD Pipeline

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main ]

jobs:
  build_next.js_(npm):
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Build Next.js (npm)
        run: |
          echo "Running Build Next.js (npm)"
          echo ""

  lint_(npm):
    runs-on: ubuntu-latest
    needs: [build_next.js_(npm)]
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Lint (npm)
        run: |
          echo "Running Lint (npm)"
          echo ""

